spring.application.name=server

server.port=8081

dataspring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true

db.host=${DB_HOST}
db.port=${DB_PORT}
db.name=${DB_NAME}
db.user=${DB_USER}
db.pass=${DB_PASS}
